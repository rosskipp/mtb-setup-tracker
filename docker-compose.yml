services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: mtb_tracker
      POSTGRES_USER: mtb
      POSTGRES_PASSWORD: mtb_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
